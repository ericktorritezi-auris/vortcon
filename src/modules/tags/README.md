# Módulo: tags

Tags globais e transversais do tenant — mesma regra das categorias (Seções 51-53, 225).

## Implementado (Estágio 7)

`tag.service.ts` — CRUD e `getTagReport` (via `getTagFlow` do Financial Engine). Sem
campo `type`/`nature`, pela mesma razão estrutural das categorias.
